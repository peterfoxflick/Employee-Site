const pool = require('../dbconnect');
const SQL = require('sql-template-strings');

function updatePic(userId, filename, callback) {
    //TODO add pic field to db. Then write this SQL call.
}

module.exports = updatePic;